services:
  - type: web
    name: cmsmatrix-backend
    runtime: node
    rootDirectory: backend
    buildCommand: npm install
    startCommand: node index.js
    envVars:
      - key: NODE_ENV
        value: production